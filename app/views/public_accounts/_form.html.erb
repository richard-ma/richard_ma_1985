<%= form_for(@public_account) do |f| %>
  <% if @public_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@public_account.errors.count, "error") %> prohibited this public_account from being saved:</h2>

      <ul>
      <% @public_account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
